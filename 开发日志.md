# U-Net 道路语义分割项目 - 开发日志

**项目路径：** `C:\Users\ASUS\Desktop\U-Net Project`

---

## 一、项目概述

基于 CamVid 数据集构建完整的 U-Net 道路语义分割项目。目标是将图像中的"道路（Road）"像素与其他类别进行二值分割。

---

## 二、初始代码编写

### 创建的文件结构

```
U-Net Project/
├── main.py          # 主入口，串联训练与可视化流程
├── config.py        # 超参数与路径配置
├── model.py         # 标准 U-Net 模型架构
├── dataset.py       # CamVid 数据集加载器（含数据增强）
├── losses.py        # 损失函数（BCE + Dice）与评估指标（IoU, Pixel Accuracy）
├── train.py         # 训练与验证循环
└── visualize.py     # 测试预测可视化与训练曲线绘制
```

### 各模块设计

| 模块 | 核心内容 |
|---|---|
| `config.py` | 图像尺寸 256×256、Batch Size 8、30 Epochs、Adam 优化器、ReduceLROnPlateau 学习率衰减 |
| `model.py` | 标准 U-Net：编码器 4 层下采样（64→1024 通道）+ 解码器 4 层上采样 + 跳跃连接，输出 1 通道 |
| `dataset.py` | 基于 Albumentations 的数据增强（水平翻转、旋转、裁剪、颜色抖动、高斯模糊、噪声），归一化至 [-1, 1] |
| `losses.py` | BCEWithLogitsLoss + DiceLoss 加权组合（各 0.5），IoU 和像素准确率指标 |
| `train.py` | 训练循环含 tqdm 进度条、验证集评估、最佳模型保存（按 val loss） |
| `visualize.py` | 随机选取测试图像，并排展示 Input / Ground Truth / Prediction，保存为 PNG |
| `main.py` | 设置随机种子、设备选择、串联训练→绘制曲线→加载最佳模型→可视化预测 |

---

## 三、根据实际 CamVid 数据集进行的适配修改

### 发现的数据集实际情况

通过读取 `CamVid/` 目录，发现实际数据集与初始代码预期存在多处差异：

| 项目 | 初始代码预期 | 实际数据集 |
|---|---|---|
| 标签目录名 | `{split}_labels/`（如 `train_labels/`） | `{split}annot/`（如 `trainannot/`） |
| 标签格式 | RGB 彩色掩膜（三通道） | **灰度索引图**（单通道 uint8，值 0-11） |
| 道路类别识别 | 通过 RGB 颜色距离匹配 | **类别索引 3 = Road** |
| class_dict.csv | 存在，用于读取颜色定义 | **不存在** |
| 文件名匹配 | 标签带 `_L` 后缀 | **图像与标签同名** |

数据集规模：训练集 367 张，验证集 101 张，测试集 233 张。

### 对 `dataset.py` 的修改

1. **移除 `class_dict.csv` 相关逻辑：** 删除 `get_road_rgb()` 函数和 `pandas` 导入，改用常量 `ROAD_CLASS_INDEX = 3` 硬编码道路类别索引。

2. **修正标签目录名：** 将 `f"{split}_labels"` 改为 `f"{split}annot"`（如 `train` → `trainannot`）。

3. **简化文件名匹配：** 移除 `_L` 后缀等多余的文件名猜测逻辑，改为直接使用同名文件匹配。

4. **重写掩膜转换逻辑：** 将 `_mask_to_binary()` 从 RGB 颜色距离匹配改为灰度索引判等（`mask == ROAD_CLASS_INDEX`）。

5. **修复 Tensor/Numpy 兼容问题：** `ToTensorV2` 会将 mask 也转为 PyTorch Tensor，添加类型检查后先转回 numpy 再进行二值化处理。

### 对 `main.py` 的修改

1. 更新文件头部注释中的数据集目录结构说明，反映实际的 `trainannot/` 命名。
2. 移除不再需要的 `pandas` 安装指令。

---

## 四、根据 `fault.md` 修复的报错与警告

### 修复的问题

| 问题 | 文件 | 修复方式 |
|---|---|---|
| `ShiftScaleRotate` 弃用警告 | `dataset.py` | 替换为 `A.Affine()`，参数等效转换（`translate_percent`、`scale`、`rotate`、`border_mode`） |
| `verbose` 参数废弃警告 | `train.py` | 改为 `verbose=False` |
| `torch.load` 安全隐患警告 | `main.py` | 添加 `weights_only=True` 参数 |

### 图片保存路径统一

- 创建 `figure/` 子目录，所有生成的图片统一保存到该目录：
  - `figure/training_history.png` — 训练曲线
  - `figure/prediction_result.png` — 测试集预测可视化
  - `figure/custom_prediction.png` — 自定义图片预测可视化
- `visualize.py` 中新增 `FIGURE_DIR` 常量，集中管理保存路径。

---

## 五、新增自定义图片推理功能

### 功能设计

为用户提供了使用训练好的模型对任意图片进行道路分割预测的能力。

### 使用方式

**方式一（最简）：** 将图片放入 `images/` 目录，直接运行：
```bash
python main.py predict
```

**方式二：** 直接指定图片路径：
```bash
python main.py predict photo1.jpg photo2.png
```

### 实现细节

- `main.py` 新增 `predict()` 函数，支持无参数（自动扫描 `images/`）和带参数（指定路径）两种模式。
- `visualize.py` 新增 `predict_custom_images()` 函数，负责推理与可视化。
- 自动检测图片是否来自 CamVid 数据集（查找 `testannot/`、`valannot/`、`trainannot/` 标签目录），有 GT 则三列展示（Input / GT / Prediction），无 GT 则两列展示（Input / Prediction）。
- 首次运行时自动创建 `images/` 目录并提示用户放入图片。

### GT 全黑 Bug 修复

**Bug 现象：** 运行 `images/` 目录中的图片时，GT 掩膜列显示为全黑。

**根因分析：** 最初的 GT 查找逻辑使用 `.replace("test", "testannot")` 的字符串替换方式。当图片放在 `images/` 目录时，`"images"` 没有被替换，导致查找路径解析到了**原图自身**（RGB 三通道）。后续 `gt == 3` 在 RGB 数组上几乎找不到匹配像素，因此显示为全黑。

**修复方式：** 改为直接拼接 `{dirname}annot` 后缀（如 `test` + `annot` = `testannot`），并增加单通道维度校验（`len(gt.shape) == 2`），确保只有真正的灰度索引标注才会被作为 GT 展示，彻底避免误匹配到 RGB 原图。

修复后两种场景均验证通过：

| 场景 | 列数 | 结果 |
|---|---|---|
| `CamVid/test/` 路径（有标注） | 3 列 | Input → GT → Prediction，GT 正确显示道路区域 |
| `images/` 路径（无标注） | 2 列 | Input → Prediction，不会误匹配 |

---

## 六、当前项目文件结构

```
U-Net Project/
├── main.py              # 主入口（训练 / 推理）
├── config.py            # 超参数与路径配置
├── model.py             # U-Net 模型定义
├── dataset.py           # CamVid 数据集加载器
├── losses.py            # 损失函数与评估指标
├── train.py             # 训练与验证循环
├── visualize.py         # 可视化（测试集 / 自定义图片 / 训练曲线）
├── best_model.pth       # 训练好的模型权重
├── prompt.md            # 任务描述文件
├── fault.md             # 报错记录文件
├── CamVid/              # 数据集目录
│   ├── train/           # 训练图像
│   ├── trainannot/      # 训练标签（灰度索引图）
│   ├── val/             # 验证图像
│   ├── valannot/        # 验证标签
│   ├── test/            # 测试图像
│   └── testannot/       # 测试标签
├── figure/              # 可视化图片输出目录
│   ├── training_history.png
│   ├── prediction_result.png
│   └── custom_prediction.png
└── images/              # 自定义推理图片目录（放入图片即可预测）
```

---

## 七、运行方式

```bash
# 安装依赖
pip install torch torchvision albumentations opencv-python matplotlib tqdm pillow

# 训练模型
python main.py

# 自定义图片推理（方式一：放入 images/ 目录）
python main.py predict

# 自定义图片推理（方式二：指定路径）
python main.py predict photo1.jpg photo2.png
```
